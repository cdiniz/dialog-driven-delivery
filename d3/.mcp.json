{
  "mcpServers": {
    "d3": {
      "command": "uv",
      "args": ["--directory", "${CLAUDE_PLUGIN_ROOT}", "run", "python", "${CLAUDE_PLUGIN_ROOT}/mcp_server/server.py"],
      "env": {
        "VIRTUAL_ENV": "",
        "D3_PROJECT_ROOT": "${PWD}"
      }
    }
  }
}
