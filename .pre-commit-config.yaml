repos:
  - repo: local
    hooks:
      - id: generate-platform-files
        name: regenerate platform files
        entry: bash -c '.venv/bin/python generate.py --all && git diff --exit-code dist/'
        language: system
        pass_filenames: false
